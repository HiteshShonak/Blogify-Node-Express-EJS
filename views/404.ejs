<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head') %>
    <style>
        /* 1. PAPER GRAIN TEXTURE (Matching Login/Signup) */
        .bg-noise {
            background-color: #F9FAFB;
            background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)' opacity='0.05'/%3E%3C/svg%3E");
        }

        .error-num {
            /* Responsive Editorial Font Size */
            font-size: clamp(8rem, 25vw, 15rem); 
            line-height: 1;
            /* Monochrome Black Gradient */
            background: linear-gradient(to bottom, #000000 40%, #6B7280 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            filter: drop-shadow(0 20px 30px rgba(0, 0, 0, 0.08));
        }

        /* Subtle Grid - lighter and cleaner */
        .grid-bg {
            background-image: radial-gradient(#e5e7eb 0.7px, transparent 0.7px);
            background-size: 30px 30px;
        }

        .float-anim {
            animation: floating 6s ease-in-out infinite;
        }

        @keyframes floating {
            0% { transform: translateY(0px); }
            50% { transform: translateY(-15px); }
            100% { transform: translateY(0px); }
        }

        .glass-btn {
            background: rgba(255, 255, 255, 0.6);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(0, 0, 0, 0.05);
        }

        .fade-in-up { animation: fadeInUp 0.8s ease-out forwards; opacity: 0; transform: translateY(20px); }
        @keyframes fadeInUp { to { opacity: 1; transform: translateY(0); } }
    </style>
</head>
<body class="bg-noise text-gray-800 flex flex-col min-h-screen antialiased grid-bg">

    <%- include('partials/nav') %>

    <main class="flex-grow flex items-center justify-center min-h-screen p-6 sm:p-12">
        
        <div class="container mx-auto text-center relative">
            
            <div class="glow-circle absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[300px] h-[300px] sm:w-[500px] sm:h-[500px] bg-gray-900/5 rounded-full blur-[80px] sm:blur-[120px] pointer-events-none"></div>

            <div class="relative z-10 fade-in-up">
                <h1 class="error-num font-black tracking-tighter float-anim">404</h1>
                
                <div class="max-w-md mx-auto -mt-4 sm:-mt-8 px-4">
                    <h2 class="text-3xl sm:text-4xl font-black text-gray-900 mb-4 tracking-tighter">Lost in <span class="opacity-50 italic">thought?</span></h2>
                    <p class="text-sm sm:text-base text-gray-500 mb-10 leading-relaxed font-medium uppercase tracking-wide text-[11px]">
                        The page you're looking for has moved to a different dimension or never existed in this one.
                    </p>

                    <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
                        <a href="/" class="w-full sm:w-auto px-10 py-4 bg-gray-900 text-white text-xs font-black uppercase tracking-widest rounded-2xl shadow-2xl hover:bg-black hover:-translate-y-1 transition-all duration-300">
                            Back to Home
                        </a>
                        <a href="/blog/all" class="w-full sm:w-auto px-10 py-4 glass-btn text-gray-900 text-xs font-black uppercase tracking-widest rounded-2xl border border-gray-200 hover:bg-white hover:-translate-y-1 transition-all duration-300">
                            Explore Stories
                        </a>
                    </div>
                </div>
            </div>

            <div class="mt-16 sm:mt-24 flex justify-center gap-6 opacity-30">
                <div class="w-1 h-1 bg-gray-900 rounded-full"></div>
                <div class="w-1 h-1 bg-gray-900 rounded-full"></div>
                <div class="w-1 h-1 bg-gray-900 rounded-full"></div>
            </div>
        </div>

    </main>

    <%- include('partials/footer') %>

</body>
</html>