<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head') %>
    <style>
        /* 1. PAPER GRAIN TEXTURE */
        .bg-noise {
            background-color: #F9FAFB;
            background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)' opacity='0.05'/%3E%3C/svg%3E");
        }

        /* 2. Black Monochrome Gradient */
        .text-gradient {
            background: linear-gradient(to right, #000000, #4B5563);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
    </style>
</head>
<body class="bg-noise text-gray-800 flex flex-col min-h-screen">

    <%- include('partials/nav') %>

    <main class="flex-grow container mx-auto px-4 mt-32 mb-20 min-h-screen">
        
        <div class="max-w-4xl mx-auto mb-16 text-center">

            <h1 class="text-4xl md:text-5xl font-black text-gray-900 mb-4 tracking-tight leading-tight">
                Explore <span class="text-gradient">Stories</span>
            </h1>
            <p class="text-gray-500 mb-10 text-lg">Search through our entire collection of ideas and thinking.</p>
            
            <form action="/blog/all" method="GET" class="relative group max-w-xl mx-auto">
                <div class="absolute inset-y-0 left-0 pl-5 flex items-center pointer-events-none">
                    <svg class="h-5 w-5 text-gray-400 group-focus-within:text-gray-900 transition-colors duration-300" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
                        <path fill-rule="evenodd" d="M8 4a4 4 0 100 8 4 4 0 000-8zM2 8a6 6 0 1110.89 3.476l4.817 4.817a1 1 0 01-1.414 1.414l-4.816-4.816A6 6 0 012 8z" clip-rule="evenodd" />
                    </svg>
                </div>
                
                <input 
                    type="text" 
                    name="search" 
                    value="<%= locals.searchQuery %>" 
                    placeholder="Search for topics, ideas, or authors..." 
                    class="block w-full pl-14 pr-12 py-4 border border-transparent rounded-full bg-white shadow-[0_8px_30px_rgb(0,0,0,0.04)] hover:shadow-[0_8px_30px_rgb(0,0,0,0.08)] focus:shadow-xl focus:ring-2 focus:ring-gray-900 focus:border-transparent placeholder-gray-400 text-gray-900 transition-all duration-300 text-lg"
                    autocomplete="off"
                >

                <% if (locals.searchQuery) { %>
                    <a href="/blog/all" class="absolute inset-y-0 right-4 flex items-center text-gray-400 hover:text-gray-900 transition-colors" title="Clear Search">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
                    </a>
                <% } %>
            </form>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            <% if (locals.blogs && locals.blogs.length > 0) { %>
                <% blogs.forEach(blog => { %>
                    <%- include('partials/blogCard', { blog: blog }) %>
                <% }) %>
            <% } else { %>
                <div class="col-span-full py-20 text-center">
                    <div class="w-20 h-20 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-6">
                        <svg class="w-10 h-10 text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg>
                    </div>
                    <h3 class="text-xl font-bold text-gray-900 mb-2">No stories found</h3>
                    <p class="text-gray-500">We couldn't find anything for "<span class="font-semibold text-gray-900"><%= locals.searchQuery %></span>".</p>
                    <a href="/blog/all" class="inline-block mt-6 px-6 py-2.5 bg-gray-900 text-white font-bold rounded-full hover:bg-black transition-all text-sm shadow-md">
                        Clear Search
                    </a>
                </div>
            <% } %>
        </div>

    </main>

    <%- include('partials/footer') %>

</body>
</html>